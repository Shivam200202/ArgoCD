apiVersion: v1
kind: Service
metadata:
  labels:
    app: argo-doom
  name: argo-doom
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 32345 
  selector:
    app: argo-doom
  type: NodePort
